#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

set -e

echo "🔁 Ejecutando npm run release antes del commit..."
if ! npm run release; then
  echo "❌ npm run release falló. Corrige los errores y vuelve a ejecutar el commit." >&2
  exit 1
fi

git add docs .docs-build-meta.json
