/**
 * Runtime Firebase configuration example.
 *
 * Copy this file to `public/firebase-config.js` (excluded from version control)
 * or upload it alongside the built assets as `firebase-config.js`, then
 * replace the placeholder strings with the real Firebase project values.
 */
window.__QB_FIREBASE_CONFIG__ = {
  apiKey: 'YOUR_FIREBASE_API_KEY',
  authDomain: 'your-project.firebaseapp.com',
  projectId: 'your-project-id',
  storageBucket: 'your-project.firebasestorage.app',
  messagingSenderId: 'YOUR_MESSAGING_SENDER_ID',
  appId: '1:YOUR_APP:web:YOUR_HASH',
}
